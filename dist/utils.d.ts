import { MetaTags, MetaTagsInput } from "./types";
declare const metaTags: ({ author, websiteDescription, websiteName, websiteTitle, coverImageLink, coverImageDescription, publishedTime, websiteLink, dataString, theme_color, twitterUserName, appIconFile, }: MetaTagsInput) => MetaTags;
export { metaTags };
